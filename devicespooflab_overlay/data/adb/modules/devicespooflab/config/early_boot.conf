# EARLY BOOT SPOOFING
# Applies critical identity props before boot completes
# FILE_ENABLED: Set to FILE_DISABLED to skip entire file
FILE_ENABLED
# Format: ENABLED,prop.name,value
# Change ENABLED to DISABLED or comment out (#) to skip

# ro.build.fingerprint
ENABLED,ro.build.fingerprint,google/cheetah/cheetah:15/AP4A.241205.013/12621605:user/release-keys

# ro.product.brand
ENABLED,ro.product.brand,google

# ro.product.model
ENABLED,ro.product.model,Pixel 7 Pro

# ro.product.name
ENABLED,ro.product.name,cheetah

# ro.product.device
ENABLED,ro.product.device,cheetah

# ro.product.manufacturer
ENABLED,ro.product.manufacturer,Google
